# ============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================================================
# Copy this file to .env.local and fill in the values
# 
# NEVER commit .env.local to version control!
# ============================================================================

# -----------------------------------------------------------------------------
# AUTHENTICATION
# -----------------------------------------------------------------------------

# Admin password for /admin routes (lead management dashboard)
# Generate a strong password and keep it secure
ADMIN_PASSWORD=your-secure-admin-password-here

# Secret for Keystatic local mode authentication
# Can be any random string for local development
KEYSTATIC_SECRET=your-keystatic-secret-here

# -----------------------------------------------------------------------------
# DATABASE (Vercel Postgres)
# -----------------------------------------------------------------------------
# These are automatically populated when you create a Vercel Postgres database
# and link it to your project. You only need to set these manually for local
# development if you want to connect to the production database.

# Main connection string (with connection pooling)
POSTGRES_URL=

# Connection string for Prisma (with connection pooling)
POSTGRES_PRISMA_URL=

# Direct connection string (without pooling, for migrations)
POSTGRES_URL_NON_POOLING=

# Individual connection parameters
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# -----------------------------------------------------------------------------
# OPTIONAL: EMAIL NOTIFICATIONS
# -----------------------------------------------------------------------------
# If you want to send email notifications when new leads are submitted,
# configure your email service here. This is optional.

# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# NOTIFICATION_EMAIL=stuartbunn59@hotmail.com

# -----------------------------------------------------------------------------
# OPTIONAL: GITHUB MODE (for production Keystatic)
# -----------------------------------------------------------------------------
# If using Keystatic in GitHub mode (recommended for production),
# configure these variables:

# KEYSTATIC_GITHUB_CLIENT_ID=
# KEYSTATIC_GITHUB_CLIENT_SECRET=
# KEYSTATIC_SECRET=

# Repository in format: owner/repo
# NEXT_PUBLIC_KEYSTATIC_GITHUB_REPO_OWNER=
# NEXT_PUBLIC_KEYSTATIC_GITHUB_REPO_NAME=
