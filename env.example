# ============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================================================
# Copy this file to .env.local and fill in the values
# 
# NEVER commit .env.local to version control!
# ============================================================================

# -----------------------------------------------------------------------------
# AUTHENTICATION
# -----------------------------------------------------------------------------

# Admin password for /admin routes (lead management dashboard)
# Generate a strong password and keep it secure
ADMIN_PASSWORD=your-secure-admin-password-here

# Secret for Keystatic local mode authentication
# Can be any random string for local development
KEYSTATIC_SECRET=your-keystatic-secret-here

# -----------------------------------------------------------------------------
# OPTIONAL: EMAIL NOTIFICATIONS
# -----------------------------------------------------------------------------
# If you want to send email notifications when new leads are submitted,
# configure your email service here. This is optional.

# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# NOTIFICATION_EMAIL=stuartbunn59@hotmail.com

# -----------------------------------------------------------------------------
# KEYSTATIC CMS - GITHUB MODE (Required for Production)
# -----------------------------------------------------------------------------
# These variables are REQUIRED for production deployments.
# The CMS automatically uses GitHub mode in production and local mode in development.
#
# Setup instructions:
# 1. Create a GitHub OAuth App at https://github.com/settings/developers
# 2. Set Homepage URL to your production domain (e.g., https://helpstonbeerfestival.com)
# 3. Set Authorization callback URL to: https://YOUR-DOMAIN/api/keystatic/github/oauth/callback
# 4. Copy the Client ID and generate a Client Secret
# 5. Generate KEYSTATIC_SECRET with: openssl rand -hex 32

# GitHub OAuth App credentials
KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=

# Random secret for session encryption (generate with: openssl rand -hex 32)
KEYSTATIC_SECRET=

# GitHub repository details
NEXT_PUBLIC_KEYSTATIC_GITHUB_REPO_OWNER=adam-cain
NEXT_PUBLIC_KEYSTATIC_GITHUB_REPO_NAME=helpston-beer-fest
